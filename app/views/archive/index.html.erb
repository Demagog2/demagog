<div class="row">
  <div class="columns">
    <h1>Archiv textů</h1>

    <% @articles.each do |article| %>
      <%= render(partial: 'article/preview', object: article, as: :article) %>
    <% end %>

    <%= paginate(@articles) %>
  </div>
</div>
